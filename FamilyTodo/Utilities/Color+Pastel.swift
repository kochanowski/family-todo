import SwiftUI

/// Pastel color constants for convenient access
/// Note: The app uses ThemeStore for dynamic theming. These are provided as convenience.
extension Color {
    // Shopping List (Purple)
    static let pastelPurple = Color(hex: "C084FC")
    static let pastelPurpleLight = Color(hex: "E9D5FF")

    // Todo (Green)
    static let pastelGreen = Color(hex: "4ADE80")
    static let pastelGreenLight = Color(hex: "BBF7D0")

    // Backlog (Yellow)
    static let pastelYellow = Color(hex: "FDE047")
    static let pastelYellowLight = Color(hex: "FEF08A")

    // Recurring (Orange)
    static let pastelOrange = Color(hex: "FB923C")
    static let pastelOrangeLight = Color(hex: "FED7AA")

    // Household (Blue)
    static let pastelBlue = Color(hex: "60A5FA")
    static let pastelBlueLight = Color(hex: "BFDBFE")

    // Areas (Pink)
    static let pastelPink = Color(hex: "F472B6")
    static let pastelPinkLight = Color(hex: "FBCFE8")

    // Settings (Gray)
    static let pastelGray = Color(hex: "9CA3AF")
    static let pastelGrayLight = Color(hex: "E5E7EB")
}
